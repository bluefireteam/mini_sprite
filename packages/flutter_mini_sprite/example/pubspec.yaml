name: flutter_mini_sprite_example
resolution: workspace
description: Example app for flutter_mini_sprite

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  dashbook: ^0.1.10
  flutter:
    sdk: flutter
  flutter_mini_sprite: ^0.1.0
  mini_sprite: ^0.2.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
